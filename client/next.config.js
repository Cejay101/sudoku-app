/** @type {import('next').NextConfig} */
const nextConfig = {
  // In production, NEXT_PUBLIC_SERVER_URL should point to your Railway backend URL.
  // In development, the socket connects directly to localhost:3001.
};

module.exports = nextConfig;
